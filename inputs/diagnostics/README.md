# Diagnostic Inputs

This directory stores sanitized task seed reports and diagnostic notes.

Rules:

- Keep the active task record in `tasks/<queue>/`.
- Keep reusable policy and specs in `docs/` or `specs/`.
- Keep private payload freezes and large runtime exports in `.foundry/workspaces/<task-id>/`.
- Record each diagnostic input in `docs/file-location-registry.json`.
